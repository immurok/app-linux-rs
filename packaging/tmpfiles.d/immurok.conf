# /run/immurok is normally created by the unit's RuntimeDirectory= (and kept
# by RuntimeDirectoryPreserve=yes). This entry recreates it on boot before the
# daemon starts, so anything that mounts or references the path is never
# looking at a directory that does not exist yet.
d /run/immurok 0755 immurok immurok -
