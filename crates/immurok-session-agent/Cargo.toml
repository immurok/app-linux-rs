[package]
name = "immurok-session-agent"
version = "0.9.0"
edition = "2021"
license = "Apache-2.0"

[[bin]]
name = "immurok-session-agent"
path = "src/main.rs"

[dependencies]
immurok-common = { path = "../immurok-common" }
tokio = { version = "1", features = ["rt", "macros", "net", "io-util", "process", "time"] }
libc = "0.2"
